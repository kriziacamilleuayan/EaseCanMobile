import React, {Component} from 'react';
import {Container, Text} from 'native-base';

export default class ProductDetailsNormal extends Component{
    render(){
        return(
            <Container>

                <Text>Product details normal</Text>
            </Container>
        )
    }
}